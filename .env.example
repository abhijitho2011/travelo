# Frontend env vars (Railway service)
# Set on the Railway service; DO NOT commit real values.

# Public API base — baked into the client bundle at build time.
VITE_API_URL=https://travelo-admin-api-production.up.railway.app/api/v1/admin

# Nitro / node-server
HOST=0.0.0.0
PORT=3000
NODE_ENV=production
